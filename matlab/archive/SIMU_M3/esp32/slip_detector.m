function [slip_flag, slip_ratio] = slip_detector(omega_est, accel, gyro, params)
% SLIP_DETECTOR  Detect wheel slip from sensor data (STUB — optional M4/M6).

    slip_flag  = false(4,1);  % STUB: no slip detected
    slip_ratio = zeros(4,1);

end
