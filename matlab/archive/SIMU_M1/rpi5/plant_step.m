function x_new = plant_step(x, tau, params, dt)
% PLANT_STEP  Mecanum AGV dynamics (STUB — returns x unchanged).
%
% Full implementation in M2: kinematics + dynamics, NO wheel slip.
% Wheel slip added in M6.

    x_new = x; % STUB: no dynamics yet

end
